import React from "react";
import { Button, Grid, Header, Segment } from "semantic-ui-react";

interface IProps{


}

const NotificationContent : React.FC<IProps> = ({}) => {
    return (
      <Grid.Column textAlign="center">
        
        
      </Grid.Column>
    );
  }
export default NotificationContent
